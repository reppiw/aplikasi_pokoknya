// App-wide constants: room names, status types, network tick rate, etc.
class AppConstants {
  // Unity Relay tick rate (packets per second)
  static const int networkTickRate = 10;

  // Idle threshold before stopping movement packets (seconds)
  static const int idleThresholdSeconds = 3;
}
