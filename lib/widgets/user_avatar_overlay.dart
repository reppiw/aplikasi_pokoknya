import 'package:flutter/material.dart';

/// HUD overlay drawn on top of the Unity view.
/// Shows floating nameplates, status chips, or other Flutter UI
/// elements positioned above each avatar.
class UserAvatarOverlay extends StatelessWidget {
  const UserAvatarOverlay({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: position nameplate widgets relative to Unity avatar world coords.
    return const Placeholder();
  }
}
